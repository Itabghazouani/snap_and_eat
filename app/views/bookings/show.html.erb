<h1 class="heading-book">Excellent choice <%= @booking.name %>!<span class="thumb">🥂</span>
<h2 class="heading-book">You're going to:</h2>
<div class="booking-show mb-2" style="background-image: linear-gradient(rgba(0,0,0,1) 0%,rgba(24,35,44,0) 19%,rgba(125,185,232,0) 100%), url(<%= asset_path(@booking.restaurant.image_url + ".jpg") %>);" >
  <h2><%= @booking.restaurant.name %></h2>
</div>
<div>
  <div class="description-container-book">
    <div class="icons-container">
      <h3 class="details-title">See your booking details</h3>
      <div class="icons-book">
        <i class="fas fa-users"></i>
          <%= @booking.guest_count %> people
      </div>
      <div class="icons-book">
        <i class="fas fa-calendar-alt"></i>
          <%= @booking.booking_date.strftime("%d %b %Y %l:%M%P") %>
      </div>
      <div class="icons-book">
        <i class="fas fa-phone"></i>
          <%= @booking.phone_number %>
      </div>
      <div class="center-position">
        <%= link_to "Check restaurant details", restaurant_path(@booking.restaurant), class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>
